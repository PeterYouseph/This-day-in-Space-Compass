version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    networks:
      - my-net

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "3001:3001"
    networks:
      - my-net
networks:
  my-net: